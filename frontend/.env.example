# FastAPI service base URL (defaults to http://localhost:8000).
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to reveal the optional NASA GIBS satellite hotspot layer
# (MODIS Terra Thermal Anomalies WMS — keyless). Off by default.
VITE_ENABLE_GIBS=false
